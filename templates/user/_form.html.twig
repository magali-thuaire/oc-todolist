{{ form_start(form, {'action' : action }) }}

    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {% if reset_password is defined %}
        <a class="reset-password__link" href="{{ path('app_reset_password_email', { 'id' : user.id}) }}">Envoyer un lien de réinitialisation de mot de passe</a>
    {% endif %}
    {{ form_row(form.role) }}
    <a href="{{ path('user_list') }}" class="btn btn-primary">Retour à la liste des utilisateurs</a>
    <button type="submit" class="btn btn-success pull-right">{{ btn_text }}</button>
{{ form_end(form) }}